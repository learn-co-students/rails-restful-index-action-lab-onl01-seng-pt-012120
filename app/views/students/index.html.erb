 
 <!DOCTYPE html>
 <html>
 <h1> index page <h1>
<% @students.each do |student| %>
  <%= student.first_name %>
  <%= student.last_name %>
  </html>
  <% end %>
    
   <%# Students.last_name.all %/>

 